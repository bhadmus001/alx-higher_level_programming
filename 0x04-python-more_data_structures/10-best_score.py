#!/usr/bin/python3
def best_score(a_dictionary):
    if a_dictionary = None:
        return None
    else:
        sorted_dic = sorted(a_dictionary, key= lambda x: x[1], reverse = True)
        return 

